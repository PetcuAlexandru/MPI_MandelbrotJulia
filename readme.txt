Mandelbrot/Julia generator using the Message Passing Interface(MPI)

Given a polynomial function f(z) = z^2 + c, the program will
determine the associated Mandelbrot/Julia set.

The calculated set will be represented as a grayscale image.

